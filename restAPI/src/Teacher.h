#pragma once

#include <string>

namespace sp
{
    struct Teacher
    {
        std::string teacherfio;
        std::string academic_degree; // Ученая степень
        std::string category; // Категория
        std::string post; // Должность
    };
}